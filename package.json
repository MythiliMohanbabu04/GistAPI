{
  "devDependencies": {
    "cypress": "^13.5.0",
    "cypress-multi-reporters": "^1.6.4",
    "mocha": "^10.2.0",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^4.3.0",
    "mochawesome-report-generator": "^6.2.0"
  },
  "scripts": {
    "cypress-headless": "npx cypress run --headless",
    "api-test": "npx cypress run --spec cypress/e2e/gist_API.cy.js",
    "web-ui": "npx cypress run --spec cypress/e2e/gist_API.cy.js",
    "cypress-headless-chrome": "npx cypress run --headless --browser chrome",
    "cypress-e2e-browser-chrome": "npx cypress run --browser chrome",
    "mochaReport": "npx cypress run --reporter mochawesome"
  }
}
